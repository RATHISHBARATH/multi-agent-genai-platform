replicaCount: 1
image:
  repository: autoscillab/api
  tag: staging
secrets:
  useExternalSecrets: true
  externalSecrets:
    PINECONE_API_KEY: /secrets/pinecone/api_key
    JWT_SECRET: /secrets/jwt/secret
